import { StyleSheet } from '@react-pdf/renderer';

export default StyleSheet.create({
  row: {
    flexGrow: 1,
    flexDirection: 'row',
  },
  column: {
    flexGrow: 1,
  },
  text: {
    margin: 10,
    fontSize: 10,
    color: 'white',
  },
});
