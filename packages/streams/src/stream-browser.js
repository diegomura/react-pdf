/* eslint-disable import/no-extraneous-dependencies */
import Writable from 'readable-stream/lib/_stream_writable';
import Readable from 'readable-stream/lib/_stream_readable';

export { Writable, Readable };
